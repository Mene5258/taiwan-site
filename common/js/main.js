'use strict'

// ローディング用js

// 4秒後にfigure要素にクラスloadedを追加する
const taiwan_logo =


  // window.addEventListener('load', 


  function () {
    const taiwan_logo = document.getElementById('loading');
    taiwan_logo.classList.add('loaded');
  }


// );


setTimeout(taiwan_logo, 4000);

// コード内のコメントアウト箇所を戻せばローディング完了時に画面が切り替わる

// ローディング用jsここまで